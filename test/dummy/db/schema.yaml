---
TestModel:
  attributes:
    name:
      type: string
      primary_ref:
      - 
  methods:
    to_s:
      returns: string
      getter: name
  has_many:
    related_models:
      dependent: destroy
      class_names:
      - RelatedModel
RelatedModel:
  belongs_to:
    test_model:
      class_names:
      - TestModel
